# APP_URL: The URL where this application is hosted.
# Automatically injected at runtime with the hosting service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
